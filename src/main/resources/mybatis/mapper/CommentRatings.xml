<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plantopia.dao.CommentRatingsMapper">

    <insert id="insertRating">
        INSERT INTO comment_ratings (rating, user_num, plccom_idx)
        VALUES (#{rating}, #{user_num}, #{plccom_idx})
    </insert>

    <update id="updateRating">
        UPDATE comment_ratings
        SET rating = #{rating}
        WHERE user_num = #{user_num} AND plccom_idx = #{plccom_idx}
    </update>

    <delete id="deleteRating">
        DELETE FROM comment_ratings
        WHERE user_num = #{user_num} AND plccom_idx = #{plccom_idx}
    </delete>

    <select id="getRatingByUser" resultType="int">
        SELECT rating FROM comment_ratings
        WHERE user_num = #{user_num} AND plccom_idx = #{plccom_idx}
    </select>

    <select id="getAverageRating" resultType="double">
        SELECT ROUND(AVG(rating), 2) FROM comment_ratings
        WHERE plccom_idx = #{plccom_idx}
    </select>
</mapper>